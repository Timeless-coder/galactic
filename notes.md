Bookings at not working. Not getting user's booked tours. Might be working.
Firebase rules
toast
styling
custom hooks and error handling
<p> styles
dates

Don't mix react-hook-form with controlled inputs.
aria-describedby pointing to non-existing id's
error typing
consistent naming of id's and such.
onChange goes inside register block
compare objects by id, like cartItem.booking.id or similar.
localStorage needs try/catch
redundant optional chaining - make sure value-confirming is consistent.
</div> rather than </p> for spacer.
Component name not matching filename and such.
Keep track of what's happening (open/changed/uploading/created) when error or such.
Fallbacks for missing variable values, like `Thanks ${currentUser?.displayName ?? 'travaler'}`
Consider state types, whether | undefined/null would be appropriate.
format inside try
type='button'
schema validate JSON.parse
existing state on error, or early return, or bailing out. Think about state of the function and app or anthing else.
watch for empty cart on receipt page
separate loading state for page load and button-click load.
watch disabled buttons based on state.
set defaults on declarations and assignments to avoid crashes.
duplicate html id's
watching dependencies in hooks
